import { useState } from 'react'; import { useAuth } from '../auth/AuthContext'; import { useNavigate } from 'react-router-dom';
export function LoginPage(){const {login}=useAuth(),nav=useNavigate();const[email,setEmail]=useState('admin@velozity.local'),[password,setPassword]=useState('Password123!'),[error,setError]=useState('');return <main className="login"><h1>Velozity</h1><p>Client project dashboard</p><form onSubmit={async e=>{e.preventDefault();try{await login(email,password);nav('/')}catch{setError('Login failed. Check your credentials.')}}}><input value={email} onChange={e=>setEmail(e.target.value)} type="email"/><input value={password} onChange={e=>setPassword(e.target.value)} type="password"/><button>Sign in</button>{error&&<p className="error">{error}</p>}</form></main>}

